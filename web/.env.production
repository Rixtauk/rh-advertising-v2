# Production environment variables for Vercel
NEXT_PUBLIC_APP_NAME="RH AI Assistant"

# API URL - will be overridden by Vercel environment variable
# For now, leaving it undefined so the app doesn't try to call localhost
# NEXT_PUBLIC_API_URL=
