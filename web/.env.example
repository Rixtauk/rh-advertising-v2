# App
NEXT_PUBLIC_APP_NAME="RH AI Assistant"
NODE_ENV=development

# FastAPI Backend URLs
# NEXT_PUBLIC_API_URL: Used by client-side code (browser)
# - In production: /api (proxied via Vercel rewrites to Railway)
# - In development: /api (proxied via Next.js rewrites to localhost:8000)
NEXT_PUBLIC_API_URL=/api

# API_URL: Used by server actions (Vercel servers)
# - Server-to-server calls need the full URL (no CORS issues)
# - In production: https://rh-advertising-v2-production.up.railway.app
# - In development: http://localhost:8000
API_URL=http://localhost:8000

# Password gate (disabled by default)
PASSWORD_GATE_ENABLED=false
PASSWORD_GATE_VALUE=changeme
