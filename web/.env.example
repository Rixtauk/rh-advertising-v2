# App
NEXT_PUBLIC_APP_NAME="RH AI Assistant"
NODE_ENV=development

# FastAPI Backend URL
# In production, this should be /api (proxied via Vercel rewrites to Railway)
# In development with local FastAPI, use http://localhost:8000 or leave as /api if using Next.js rewrites
NEXT_PUBLIC_API_URL=/api

# Password gate (disabled by default)
PASSWORD_GATE_ENABLED=false
PASSWORD_GATE_VALUE=changeme
